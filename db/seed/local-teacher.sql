-- =====================================================================
--  로컬 개발용 강사 계정  ★ 서버 배포 절차에서는 절대 돌리지 않는다
-- ---------------------------------------------------------------------
--  db/schema.sql 은 계정을 심지 않는다. 왜인지는 그 파일 끝의 주석에 있다.
--  요약하면 Teacher 행이 하나라도 있으면 최초 설정 화면(/setup)이 영구히 닫히고,
--  유일한 계정이 저장소에 적힌 값이 된다. 그 계정은 ADMIN 이라
--  파기 목록과 대시보드에서 퇴원생 실명까지 본다.
--
--  그래서 서버·스테이징에서는 /setup 으로 첫 계정을 만든다.
--  이 파일은 로컬에서 계정을 하나 더 끼워 넣어야 할 때만 쓴다.
--
--  비밀번호 해시를 이 파일에 박아 두지 않는다.
--  저장소에 들어간 해시는 지운 뒤에도 git 이력에 남고, 그 값을 그대로 쓴 서버가
--  하나라도 있으면 아이디와 비밀번호를 아무나 아는 상태가 된다.
--  그래서 해시는 각자 만들어 아래 <BCRYPT_해시> 자리에 넣는다.
--
--  해시 만들기 (Spring Security 의 BCryptPasswordEncoder 와 같은 형식)
--    htpasswd -bnBC 10 "" '고른비밀번호' | tr -d ':\n'
--    또는 앱을 띄워 /setup 으로 계정을 만들고 그 행의 password 를 복사한다.
--
--  실행 (스키마 이름을 박지 않는다. 접속한 DB 에 그대로 들어간다)
--    mysql -u <user> -p <DB이름> < db/seed/local-teacher.sql
-- =====================================================================

INSERT INTO Teacher (loginId, password, name, role, active)
VALUES ('local', '<BCRYPT_해시>', '로컬', 'ADMIN', 1);
